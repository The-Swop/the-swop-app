{
  "name": "swop-frontend",
  "version": "0.6.3",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "setup": "npm install && npm audit fix && dfx start --clean --background && dfx canister create --all && dfx deploy && dfx stop && echo 'Ready to go!'",
    "deploy-ic": "dfx deploy --network ic"
  },
  "dependencies": {
    "@dfinity/agent": "0.19.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@types/react": "^17.0.20",
    "@types/react-dom": "^17.0.9",
    "@vitejs/plugin-react-refresh": "^1.3.6",
    "buffer": "^6.0.3",
    "typescript": "^4.4.3",
    "vite": "^2.5.7"
  }
}